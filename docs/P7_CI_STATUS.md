# P7 CI Status

P7 requires GitHub Actions evidence before release-ready status.

The repository currently contains one CI workflow with backend tests, frontend build, and Docker Compose validation. A missing workflow run is explicitly treated as **not verified**, not as a pass.

This document is intentionally not marked green until GitHub Actions produces successful run evidence for the P7 commit/PR.
